import React from "react";

import Paragraph from "../paragraph";

export default { title: "Paragraph" };

export const defaultOptions = () => {
  return (
    <Paragraph content="This is a paragraph component with default options." />
  );
};
